

        =__
        toe
        /

    small js-inspired language, based on prototypes and braces.

      let Animal = Object.clone();
      Animal.init = fn(legs) {
        this.legs = legs;
      };
      let PetDog = Animal.clone();
      PetDog.init = fn(name) {
        super.init(4);
        this.name = name;
      };
      let dog = PetDog.new("阿福");


