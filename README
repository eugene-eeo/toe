

        =__
        toe
        /

    small js-inspired language, based on prototypes and braces.

      let Animal = Object.clone(nil);
      Animal.init = fn(legs) {
        this.legs = legs;
      };
      let PetDog = Object.clone(Animal);
      PetDog.init = fn(name) {
        super.init(4);
        this.name = name;
      };
      let dog = PetDog.new("阿福");


